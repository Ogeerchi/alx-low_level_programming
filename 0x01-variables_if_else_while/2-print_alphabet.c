#include <stdio.h>

/**
 * main - is the starting point of the program
 * Return: value is zero
 **/

int main(void)
{
	/* we code */
	char a;
	for (a = 'a'; a <= 'z'; a++)
		putchar(a);
	putchar('\n');
	return (0);
}
