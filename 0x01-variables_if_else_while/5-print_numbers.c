#include <stdio.h>

/**
 * main - program startibg point
 * Return: returns 0 value
 **/

int main(void)
{
	char i;

		for (i = '0'; i <= '9'; i++)
		{
			putchar(i);
		}
		putchar('\n');
		return (0);
}
